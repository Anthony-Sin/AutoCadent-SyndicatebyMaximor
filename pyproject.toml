[project]
name = "autocadent"
version = "0.1.0"
description = "Evaluated robot CAD and board workspace"
requires-python = ">=3.11,<3.13"
dependencies = ["cadquery==2.6.1", "mcp>=1.12,<2", "fastapi>=0.115,<1", "uvicorn>=0.34,<1"]
[dependency-groups]
dev = ["pytest>=8,<9", "httpx>=0.28,<1"]
