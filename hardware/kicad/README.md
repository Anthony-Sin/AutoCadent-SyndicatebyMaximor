# KiCad MCP working directory

The generated board is `../../web/artifacts/board/rove-1.kicad_pcb`. The project-scoped MCP probe uses this directory and passes that explicit path. Regenerate with `/usr/bin/python3 scripts/board.py` from the repository root.
