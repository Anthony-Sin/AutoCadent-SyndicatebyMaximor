# Server only. Never place credentials in web/ or commit a populated env file.
TENSORMUX_API_KEY=
# Optional server process settings (keep secrets only in the private .env):
# AUTOCADENT_ENV_FILE=/absolute/path/to/private/.env
# TENSORMUX_BASE_URL=https://api.tensormux.com/v1
# TENSORMUX_MODEL=glm-4-7-flash
# TENSORMUX_TIMEOUT_SECONDS=90
